================================================================================
COPY-PASTE PROMPTS PARA CLAUDE OPUS - MERCADINHOSYS
================================================================================

================================================================================
PROMPT 1: DASHBOARD COMPLETO
================================================================================

Arquivo: frontend/mercadinhosys-frontend/src/features/dashboard/DashboardPage.tsx

Renderizar 6 seções que backend retorna em GET /api/dashboard/cientifico?days=30:

1. Análise Detalhada (após Visão Geral, linha ~720):
   - Curva ABC: 3 cards (A, B, C) com barras de progresso
   - RFM: 3 números (Recência, Frequência, Monetário)

2. Análise Temporal:
   - LineChart com tendência de vendas (2 linhas: total, quantidade)

3. Insights Científicos:
   - Cards vermelhos para anomalias (clicáveis)
   - Cards azuis para recomendações (clicáveis)

4. RH:
   - 3 cards: funcionários_ativos, horas_trabalhadas, folha_pagamento

5. Fiados:
   - 3 cards: total_fiado (orange), contas_vencidas (red), clientes_fiado

Modais já existem em: src/features/dashboard/components/modals/

Aceite: 6 seções renderizam, dados do backend, modais funcionam, <2s, sem erros

================================================================================
PROMPT 2: MÚLTIPLOS PAGAMENTOS - BACKEND
================================================================================

Arquivo: backend/app/routes/pdv.py (linha ~747)

Completar finalizar_venda() após criar nova_venda:

1. Criar Pagamento (um por forma)
   - Validar: soma = total venda
   - Salvar: forma, valor, referencia, data

2. Se fiado, criar ContaReceber
   - Valor, data_vencimento (+30 dias padrão)
   - Atualizar cliente.saldo_devedor

3. Atualizar estoque
   - Lock pessimista (with_for_update)
   - Validar quantidade
   - Criar MovimentacaoEstoque

4. Registrar MovimentacaoCaixa (apenas formas != 'fiado')

5. Commit atômico com rollback em erro

Modelos necessários:
- Pagamento(venda_id, forma_pagamento, valor, referencia)
- ContaReceber(cliente_id, venda_id, valor, data_vencimento, status)
- MovimentacaoEstoque(produto_id, tipo, quantidade, venda_id)
- MovimentacaoCaixa(caixa_id, tipo, valor, venda_id)
- Cliente: adicionar saldo_devedor, limite_credito

Aceite: 1 forma OK, 2+ formas OK, fiado OK, estoque OK, caixa OK, testes OK

================================================================================
PROMPT 3: SEED - MÚLTIPLOS PAGAMENTOS
================================================================================

Arquivo: backend/seed_simulation_master.py

Adicionar função: criar_vendas_com_multiplos_pagamentos()

Para cada mês: 10 vendas com 2-3 formas (dinheiro, cartão, pix, fiado)

Exemplo venda R$100:
- Dinheiro: R$30
- Cartão: R$50
- Fiado: R$20 (cria ContaReceber)

Criar registros Pagamento (um por forma)
Se fiado: criar ContaReceber + atualizar saldo_devedor cliente

Chamar em MasterSeeder.seed()

Testar:
sqlite3 backend/instance/mercadinho_local.db \
  "SELECT v.id, COUNT(p.id) FROM vendas v LEFT JOIN pagamentos p ON v.id=p.venda_id GROUP BY v.id HAVING COUNT(p.id)>1 LIMIT 5;"

Aceite: 100+ vendas múltiplos pagamentos, pagamentos somam correto, ContaReceber OK, saldo_devedor OK

================================================================================
PROMPT 4: DELIVERY - INTERFACE
================================================================================

Arquivo: frontend/mercadinhosys-frontend/src/features/delivery/DeliveryPage.tsx

Implementar interface delivery com:

1. DeliveryList (GET /delivery/entregas)
   - Filtros: pendente, em_rota, entregue
   - Tabela: ID, cliente, motorista, status, data

2. DeliveryForm (POST /delivery/entregas)
   - Selecionar venda, motorista, veículo, data

3. Status Update (PUT /delivery/entregas/{id})
   - Botões: Pendente → Em Rota → Entregue

4. Rastreamento (GET /delivery/rastreamento/{id})
   - Localização motorista, timeline status

Backend endpoints já existem em: backend/app/routes/delivery.py

Aceite: Listar OK, criar OK, atualizar status OK, rastrear OK, integração backend OK

================================================================================
PROMPT 5: NOVO CLIENTE - ONBOARDING
================================================================================

Teste manual: Novo cliente usa sistema do zero

1. Registrar: email teste@novo.com, senha Teste123!, CNPJ 12.345.678/0001-90
2. Login
3. Criar cliente (nome, CPF, email, telefone)
4. Criar produto (nome, código barras, preço, quantidade)
5. Fazer venda (buscar, carrinho, pagar dinheiro)
6. Ver dashboard (deve mostrar venda)
7. Ver relatórios (deve mostrar dados)

Aceite: Registro OK, login OK, cliente OK, produto OK, venda OK, dashboard OK, relatórios OK

================================================================================
PROMPT 6: REFATORAÇÃO - CÓDIGO LIMPO
================================================================================

Criar 4 arquivos:

1. backend/app/services/venda_service.py
   - validar_estoque(produto_id, quantidade)
   - criar_pagamentos(venda_id, pagamentos_data)
   - criar_conta_receber(cliente_id, venda_id, valor, data_vencimento)
   - atualizar_estoque(produto_id, quantidade, venda_id)

2. backend/app/utils/errors.py
   - APIError(code, message, status_code, details)
   - EstoqueInsuficienteError
   - ClienteNaoEncontradoError
   - CaixaFechadoError

3. backend/app/utils/response.py
   - APIResponse.success(data, message, status_code)
   - APIResponse.error(code, message, details, status_code)
   - APIResponse.paginated(items, page, per_page, total)

4. backend/app/schemas/venda_schema.py
   - FinalizarVendaSchema (Pydantic)
   - PagamentoSchema
   - VendaItemSchema

Refatorar finalizar_venda() para usar esses serviços (50 linhas ao invés de 150)

Aceite: Código limpo, sem duplicação, testes OK, performance igual

================================================================================
PROMPT 7: TESTES - VALIDAÇÃO
================================================================================

Criar: backend/tests/test_vendas.py

Testes:
1. test_venda_simples() - 1 forma, estoque OK, caixa OK
2. test_multiplos_pagamentos() - 2+ formas, soma OK, cada pagamento OK
3. test_fiado() - ContaReceber OK, saldo_devedor OK
4. test_estoque_insuficiente() - Erro OK, rollback OK
5. test_caixa_fechado() - Erro OK
6. test_dashboard_completo() - 6 seções OK, dados OK

Executar: pytest backend/tests/test_vendas.py -v

Aceite: 6 testes passam, cobertura >80%, sem warnings

================================================================================
COMO USAR ESTES PROMPTS
================================================================================

1. Copie o prompt desejado (entre as linhas de ===)
2. Cole no Claude Opus
3. Adicione contexto se necessário (abra arquivo mencionado)
4. Execute as tarefas
5. Teste conforme critério de aceite
6. Faça commit

Ordem recomendada:
Dia 1: Prompts 1 + 2
Dia 2: Prompts 3 + 4
Dia 3: Prompts 5 + 6
Dia 4: Prompt 7 + correções

Tempo total: 24-31 horas

================================================================================
DICAS
================================================================================

- Sempre cite linhas exatas onde adicionar código
- Teste após cada prompt
- Faça commits frequentes
- Atualize docstrings
- Verifique sem erros no console

================================================================================
