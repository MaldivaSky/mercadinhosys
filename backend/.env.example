# FLASK
FLASK_ENV=production
FLASK_APP=run.py

# DATABASE
# Use em produção (Render/Neon): defina DATABASE_URL nas variáveis do serviço
# Exemplo (NÃO COMITE CREDENCIAIS):
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=
# Para desenvolvimento local, use um SQLite fora do repositório
# SQLITE_DB=sqlite:///mercadinho.db
SQLITE_DB=

# SECURITY - CRÍTICO PARA PRODUÇÃO
# ⚠️  NUNCA use valores padrão em produção!
# Gere chaves seguras com: openssl rand -hex 32
SECRET_KEY=your-super-secure-random-secret-key-here
JWT_SECRET_KEY=your-jwt-super-secure-random-secret-key-here

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# EMAIL
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# OPTIONAL
LOG_LEVEL=INFO
